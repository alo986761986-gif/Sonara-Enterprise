name: Deploy SONARA Music + Blackwell Video AI

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/deploy-cloudflare-video-recovery.yml"
      - "cloudflare/sonara-web-v15-router.mjs"
      - "cloudflare/sonara-video-ui-edge.mjs"
      - "cloudflare/sonara-video-ui-edge.test.mjs"
      - "index.html"
      - "wrangler.jsonc"
      - "cloudflare/sonara-ab-player-visibility-edge.mjs"
      - "cloudflare/sonara-native-auth-safe.mjs"
      - "cloudflare/sonara-native-video-state.mjs"
      - "cloudflare/sonara-video-native-route.test.mjs"
      - "cloudflare/sonara-native-video-state.test.mjs"
      - "api/video/provider.ts"
      - "src/components/video/VideoAISectionControl.tsx"
      - "src/server/video/provider-molab-fast-profile.test.ts"
      - "scripts/molab-sonara-wan22-video-worker.py"
      - "cloudflare/sonara-engine-v15-authoritative-prompt.mjs"
      - "cloudflare/sonara-engine-v15-authoritative-prompt.test.mjs"
      - "cloudflare/extreme-fidelity-v16.test.mjs"
      - "scripts/activate-extreme-fidelity-v16.cjs"
      - "cloudflare/sonara-engine-v17-lm-composer.mjs"
      - "cloudflare/sonara-engine-v18-fast-hq.mjs"
      - "cloudflare/sonara-engine-v19-resilient-dual.mjs"
      - "cloudflare/sonara-web-generator-stability.mjs"
      - "scripts/kaggle-sonara-music-v20-dual-t4.py"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cloudflare-sonara-dual-t4-music-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy music and activate Blackwell Video AI
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN || secrets.CF_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID || secrets.CF_ACCOUNT_ID }}

    steps:
      - name: Checkout SONARA
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Install dependencies
        run: npm install

      - name: Resolve Cloudflare credentials
        shell: bash
        run: |
          set -euo pipefail
          if [[ -z "${CLOUDFLARE_API_TOKEN}" ]]; then
            echo "Missing Cloudflare API token." >&2
            exit 1
          fi
          account_id="${CLOUDFLARE_ACCOUNT_ID}"
          if [[ -z "${account_id}" ]]; then
            response="$(curl -fsS -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" -H "Content-Type: application/json" "https://api.cloudflare.com/client/v4/zones?name=sonaraenterprise.com&per_page=50")"
            account_id="$(CF_RESPONSE="${response}" node - <<'NODE'
          const payload = JSON.parse(process.env.CF_RESPONSE || '{}');
          const zone = Array.isArray(payload.result) ? payload.result.find(item => item?.name === 'sonaraenterprise.com') : null;
          process.stdout.write(String(zone?.account?.id || ''));
          NODE
            )"
          fi
          test -n "${account_id}"
          echo "::add-mask::${account_id}"
          echo "CLOUDFLARE_ACCOUNT_ID=${account_id}" >> "${GITHUB_ENV}"

      - name: Verify Blackwell Video AI active proxy
        shell: bash
        run: |
          set -euo pipefail
          node cloudflare/sonara-video-active-proxy.test.mjs
          node cloudflare/sonara-video-ui-edge.test.mjs
          node cloudflare/sonara-video-native-route.test.mjs
          node cloudflare/sonara-native-video-state.test.mjs
          npx tsx src/server/video/provider-molab-fast-profile.test.ts
          grep -q "return proxyActiveVideoApi(request)" cloudflare/sonara-web-v15-router.mjs
          grep -q "active-molab-wan22-blackwell" cloudflare/sonara-web-v15-router.mjs
          ! grep -q "videoSuspendedResponse" cloudflare/sonara-web-v15-router.mjs
          echo "Video AI routes through the authenticated Vercel backend to MoLab Wan 2.2."

      - name: Validate Real Prompt v21 music engine
        run: |
          node cloudflare/sonara-engine-v15-authoritative-prompt.test.mjs
          node scripts/activate-extreme-fidelity-v16.cjs
          node cloudflare/extreme-fidelity-v16.test.mjs
          grep -q "dual-t4-max-speed-v21" cloudflare/sonara-engine-v19-resilient-dual.mjs
          grep -q "thinking: true" cloudflare/sonara-engine-v19-resilient-dual.mjs
          grep -q "constrained_decoding: true" cloudflare/sonara-engine-v19-resilient-dual.mjs
          grep -q "if (pool.length >= 2)" cloudflare/sonara-engine-v19-resilient-dual.mjs
          grep -q "Promise.all" cloudflare/sonara-engine-v19-resilient-dual.mjs
          echo "Real Prompt v21 dual-worker parallel path verified."

      - name: Build production app
        run: npm run build

      - name: Deploy Worker to Cloudflare production
        run: npx --yes wrangler@latest deploy --config wrangler.jsonc

      - name: Verify Video AI progress fallback is public
        shell: bash
        run: |
          set -euo pipefail
          for attempt in {1..18}; do
            script="$(curl -fsS --max-time 30 -H 'Cache-Control: no-cache' "https://sonaraenterprise.com/sonara-video-ui-edge.js?v=3&verify=${GITHUB_SHA}-${attempt}" || true)"
            html="$(curl -fsS --max-time 30 -H 'Cache-Control: no-cache' "https://sonaraenterprise.com/?video-progress=${GITHUB_SHA}-${attempt}" || true)"
            if grep -q "estimated-edge-fallback-v1" <<<"${script}" && grep -q "sonara-video-ui-edge.js?v=3" <<<"${html}"; then
              echo "Video AI legacy 55% progress fallback is public."
              exit 0
            fi
            sleep 5
          done
          echo "Video AI progress fallback did not become public." >&2
          exit 1

      - name: Verify Blackwell Video AI route is live
        shell: bash
        run: |
          set -euo pipefail
          for attempt in {1..18}; do
            code="$(curl -sS --max-time 30 -D /tmp/video-active.headers -o /tmp/video-active.json -w '%{http_code}' -H 'Cache-Control: no-cache' "https://sonaraenterprise.com/api/video/health?active=${GITHUB_SHA}-${attempt}" || true)"
            state="$(awk 'BEGIN{IGNORECASE=1} /^x-sonara-video-state:/{print $2}' /tmp/video-active.headers | tail -n1 | tr -d '\r')"
            edge="$(awk 'BEGIN{IGNORECASE=1} /^x-sonara-video-edge:/{print $2}' /tmp/video-active.headers | tail -n1 | tr -d '\r')"
            if [[ "${code}" == "401" && "${state}" == "active-molab-wan22-blackwell" && "${edge}" == "vercel-auth-proxy-v1" ]] && node - <<'NODE'
          const fs = require('fs');
          const h = JSON.parse(fs.readFileSync('/tmp/video-active.json','utf8'));
          const ok = h?.error?.code === 'AUTH_REQUIRED';
          console.log(JSON.stringify(h));
          process.exit(ok ? 0 : 1);
          NODE
            then
              echo "Blackwell Video AI authenticated route verified in production."
              exit 0
            fi
            sleep 5
          done
          echo "Blackwell Video AI route did not become active in production." >&2
          exit 1

      - name: Verify production music health
        shell: bash
        run: |
          set -euo pipefail
          response="$(curl -fsS --max-time 30 -H 'Cache-Control: no-cache' "https://api.sonaraenterprise.com/api/health?music=${GITHUB_SHA}")"
          printf '%s' "${response}" > /tmp/music-health.json
          node - <<'NODE'
          const fs = require('fs');
          const health = JSON.parse(fs.readFileSync('/tmp/music-health.json', 'utf8'));
          const workers = Array.isArray(health.aceStepWorkers) ? health.aceStepWorkers : [];
          const count = Number(health.aceStepWorkerCount || workers.length || 0);
          if (!(count > 0 && workers.some(item => item?.kind === 'kaggle'))) {
            throw new Error(`Kaggle ACE-Step worker unavailable: ${JSON.stringify(health)}`);
          }
          if (!(health.stableDual === true
            && health.stableDualProfile === 'dual-t4-studio-max-8step-v25'
            && health.sonaraMusicV17 === true
            && health.audioQualityJudge === 'sonara-quality-gate-v2'
            && health.sonaraNextStudio?.version === 'sonara-studio-v2.0-production')) {
            throw new Error(`SONARA official dual T4 / Studio chain not active: ${JSON.stringify(health)}`);
          }
          console.log(JSON.stringify({status: health.status, aceStepWorkerCount: count, aceStepWorkers: workers, stableDualProfile: health.stableDualProfile, audioQualityJudge: health.audioQualityJudge, studioVersion: health.sonaraNextStudio?.version}));
          console.log('Music remains healthy with both Kaggle T4 workers, Quality Gate v2 and Studio 2.0 active.');
          NODE
